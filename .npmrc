# plans/live-workers DESIGN §5: never install the Agent SDK's peers (zod, an MCP server, ~95 packages)
# or its optional bundled 222 MB claude. The [] form makes omit a list; a repeated plain `omit=` keeps only the last.
omit[]=peer
omit[]=optional
save-exact=true
