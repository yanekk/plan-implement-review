{
  "name": "parallel-pir",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Coordinator for running PIR tasks in parallel across background workers. Pure core, thin shell.",
  "engines": {
    "node": ">=22.19"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.282",
    "@earendil-works/pi-tui": "0.87.1"
  },
  "scripts": {
    "test": "FORCE_COLOR=0 NO_COLOR=1 node --test --test-reporter=dot 'src/**/*.test.mjs'"
  }
}
